import React from 'react'
// import styled, {keyframes} from 'styled-components'
import Box from './Box'

export default ({ themes, show }) => {
  return (
    <Box show={show}>
      <h1>Favorites</h1>
    </Box>
  )
}
